<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   
    <title>Espace admin</title>
    
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path("admin_home_index") }}"><img src="{{ asset("img/logo.jpg") }}" alt="logo" style="border-radius: 10%;"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    {% if app.user %}
                        <span class="nav-link">Connecté en tant que {{ app.user.getFirstName() }} {{ app.user.getLastName() }}</span>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("vers_accueil_index") }}">Retour au site</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("user_home_index") }}">Espace privé</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_logout") }}">Déconnexion</a>
                </li>                
            </ul>
        </div>
    </div>
</nav>


{% block main %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card mt-5">
                    <div class="card-header text-center">
                        <h1 class="card-title">Bienvenue dans l'Espace Admin</h1>
                    </div>
                    <div class="card-body">
                        <p class="lead">Vous êtes connecté en tant qu'administrateur. Vous pouvez contrôler les fonctionnalités suivantes :</p>
                        <div class="list-group">
                            <a href="{{ path('admin_bungalow_index') }}" class="list-group-item list-group-item-action">Gérer les Bungalows</a>
                            <a href="{{ path('app_formule_index') }}" class="list-group-item list-group-item-action">Gérer les Formules</a>
                            <a href="{{ path('app_option_index') }}" class="list-group-item list-group-item-action">Gérer les Options</a>
                            <a href="{{ path('app_image_index') }}" class="list-group-item list-group-item-action">Gérer les Images</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>



    

    

    
