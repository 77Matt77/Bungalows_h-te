<!DOCTYPE html>

<title>Edit Calendrier</title>

{% block body %}
    <h1>Edit Calendrier</h1>

    {{ include('calendrier/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_calendrier_index') }}">back to list</a>

    {{ include('calendrier/_delete_form.html.twig') }}
{% endblock %}
