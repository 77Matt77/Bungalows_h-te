{% extends "bases/admin.html.twig" %}

{% block title %}Liste des catégories{% endblock %}

{% block description %}Liste des catégories{% endblock %}

{% block main %}
    <div class="container mt-4">
        <h1 class="text-center">Modifier la Formule</h1>

        <div class="row mt-4">
            <div class="col-md-6 offset-md-3">
                {{ include('formule/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-6 offset-md-3 text-center">
                <a href="{{ path('app_formule_index') }}" class="btn btn-primary">Retour à la liste</a>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-6 offset-md-3">
                {{ include('formule/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

